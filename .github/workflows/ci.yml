name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          pip install -e ".[dev]"

      - name: Lint with ruff
        run: ruff check src/ tests/

      - name: Run unit & provider tests
        env:
          PYTHONPATH: .
        run: pytest tests/test_core.py tests/test_providers.py -v --tb=short

      - name: Run API tests
        env:
          PYTHONPATH: .
        run: pytest tests/test_api.py -v --tb=short

      - name: Run integration tests (with mock bot)
        env:
          PYTHONPATH: .
        run: |
          python -m tests.mock_bot_server &
          sleep 2
          pytest tests/test_integration.py -v --tb=short
          kill %1 2>/dev/null || true

  # AI Simulation Tests (run on PRs to main)
  simulation-test:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -e .

      - name: Run simulation tests
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          simtest run \
            --bot-endpoint ${{ secrets.BOT_ENDPOINT }} \
            --personas 10 \
            --max-turns 5 \
            --output ./reports
        continue-on-error: true

      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: simulation-report
          path: reports/